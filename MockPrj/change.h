#ifndef __CHANGE_H__
#define __CHAGNE_H__
#include "common.h"
wchar_t * MergeNameFile(FILE *, char *fileName, char *extensionFileName);
int stringToNum(char **key);// from string chang to number to switch case
#endif