#include<iotream> 
using namespace std;
const maxCard =  100;
enum Bool {false, true};
class set {
	int x, y;
	public:
		point(int a, int b);
		void offsetPt(int u, int v);
	
};
inline set::point(int a, int b) {
	a = x;
	b = y;
} //cai thien toc do
friend // truy cap private
friend void inset::
	//doi so mac dinh
	
